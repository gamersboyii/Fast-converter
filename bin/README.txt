Place Windows x64 ffmpeg.exe and ffprobe.exe here before packaging.

SIZE TIP:
Use a slim / essentials static build (usually 30-70 MB).
Full shared builds or builds with many optional libraries can push the final
installer past 1 GB. Only the two .exe files are required.
